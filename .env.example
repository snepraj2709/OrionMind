# Required for deployed environments while temporary mock authentication is enabled.
ORION_MOCK_AUTH_SECRET=

# Leave empty to use the same-origin Next.js mock endpoints. Set this to the
# real backend origin, without a trailing slash, when integrating the API.
NEXT_PUBLIC_API_BASE_URL=
